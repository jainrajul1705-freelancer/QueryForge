# ============================================================
# QueryForge — SQL Query Generator using SQLCoder-7B-2
# Requirements File
# Python 3.9+
# ============================================================

# ── Core ML Framework ─────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── HuggingFace Ecosystem ─────────────────────────────────────
transformers>=4.38.0
accelerate>=0.27.0
huggingface_hub>=0.21.0
tokenizers>=0.15.0
safetensors>=0.4.0

# ── Quantization (4-bit / 8-bit GPU support) ──────────────────
bitsandbytes>=0.42.0

# ── Dataset Loading ───────────────────────────────────────────
datasets>=2.17.0
pyarrow>=14.0.0

# ── Tokenizer Dependencies ────────────────────────────────────
sentencepiece>=0.1.99
protobuf>=3.20.0

# ── Gradio UI ─────────────────────────────────────────────────
gradio>=4.20.0
gradio-client>=0.11.0

# ── Environment & Config ──────────────────────────────────────
python-dotenv>=1.0.0

# ── Utilities ─────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
requests>=2.31.0
packaging>=23.0
filelock>=3.12.0
